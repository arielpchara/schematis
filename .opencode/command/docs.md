---
description: Sync function catalog and HTML docs after a public API change
agent: build
---

Load the `doc-updater` skill, then sync docs.

$ARGUMENTS

Update the matching `docs/*.md` catalog and its `.html` twin. Do not commit.
