---
description: Bump package.json using semver (major / minor / patch)
agent: build
---

Load the `version-bump` skill, then bump from the `origin/main` diff.

$ARGUMENTS

Follow the skill. Do not bump for OpenCode skills. Do not tag. Commit the version files only if the user asked to commit.
